enum ActivityType { directSow, preCulture, seedSaving }

enum ActivityStatus { starts, continues, ends }

enum RelevancePhase { newPhase, ongoing, ending, none }

enum Category {
  fruchtgemuese,
  kuerbisartige,
  kohlgewaechse,
  blattgemueseSalat,
  leguminosen,
  sonstigesGemuese,
  kraeuter,
  blumen,
  unknown,
}

enum TubeColor { red, green, blue, yellow, white }
