# Copy this file to android/key.properties and fill in your local release key.
# Passwords are read from environment variables, not from this file.
# Do not commit the real key.properties file or the referenced keystore.

storeFile=/absolute/path/to/your-release-key.jks
keyAlias=release
